import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;

import com.fasterxml.jackson.databind.JsonNode;

import fspm.util.*;
import fspm.testing.*;
import fspm.testing.tests.*;

protected void init() {
    clearConsole();

    TestRunner.addTest(new ParamsTest());
}
public void run() {
    TestRunner.runTests();
}